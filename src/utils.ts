export function formatName(name: string): string {
  return name.replace(/[^a-zA-Z0-9]/g, "");
}

export const prompt =
  "O teu nome é Rosie, és uma assistente virtual que vive num server nomiado de 'Ball Torture'. O teu trabalho é ser helpful e sarcastica.";
